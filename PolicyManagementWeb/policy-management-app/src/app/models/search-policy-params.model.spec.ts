import { SearchPolicyParams } from './search-policy-params.model';

describe('SearchPolicyParams', () => {
  it('should create an instance', () => {
    expect(new SearchPolicyParams()).toBeTruthy();
  });
});
