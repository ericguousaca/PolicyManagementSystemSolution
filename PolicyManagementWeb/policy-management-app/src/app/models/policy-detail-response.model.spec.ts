import { PolicyDetailResponse } from './policy-detail-response.model';

describe('PolicyDetailResponse', () => {
  it('should create an instance', () => {
    expect(new PolicyDetailResponse()).toBeTruthy();
  });
});
